container_commands:
  01_generate_migration:
    command: "source /var/app/venv/*/bin/activate && flask db migrate -m 'auto_migration_on_deploy' || true"
    leader_only: true
  02_apply_migration:
    command: "source /var/app/venv/*/bin/activate && flask db upgrade"
    leader_only: true
